language: c++
sudo: required
before_install:
  - sudo apt-get install g++

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
  - ./bin/ceedukotests

after_success:
  - bash <(curl -s https://codecov.io/bash)
